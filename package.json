{
  "name": "todolist-pwa",
  "version": "1.0.0",
  "description": "TodoList PWA 应用",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open:android": "npx cap open android",
    "cap:build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
