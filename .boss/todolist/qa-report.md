# QA 测试报告（阶段性）

## 1. 范围
- 前端原型页面：列表与看板、搜索输入、PWA 离线缓存（基础）；
- 后端接口：健康检查、鉴权（注册/登录）、Todos CRUD、子任务、标签、提醒（代码级实现）。

## 2. 核心用例（抽样结果）
- Web 预览加载成功（http://localhost:8000/），布局与交互正常；
- 看板三列渲染与卡片拖动（原型层）表现正常；
- PWA manifest 与 service worker 注册成功；基础资源可离线获取；
- 后端接口契约与路由挂载完整；模型与会话层初始化脚本可创建表；

## 3. 覆盖率与门禁
- 单元/集成/E2E 覆盖率目标≥70%（待持续完善）；
- 当前阶段门禁：UI/A11y 检查基本通过；无高危问题记录。

## 4. 问题与改进建议
- 后端运行环境需安装依赖以执行在线测试与 E2E；建议容器化统一环境；
- IndexedDB 增量同步策略后续完善；\n
- 看板拖拽在移动端实现长按交互需引入手势库（后续）；

